# JOINTRUN Audit — 2026-07-14

STEP 0 of `JOINTRUN_마스터플랜_v3_실행지침서.md`. Read-only — no code changed while producing this document.

Scope: the 8 items in the master plan's **Sprint Definition of Done**, checked against the actual repo state on `main` (commit `1b80336`) as of this audit.

---

## 1. HOME이 기록 중심으로 변경됨 (HOME is record-centric)

**상태: 완료**

- `src/components/tabs/HomeModule.jsx`, `src/components/tabs/home/FirstScanHomeState.jsx` — card order is `RelativeChangeCard → PatternInsightCard → TodayActionCard → TodayStatusCard (link-out, no raw score) → RecordSection → RecentTimelinePreview`.
- `src/components/tabs/home/TodayStatusCard.jsx` — no longer renders the raw Finger Score number; it's a tap-through card into REPORT (`오늘의 정밀 지표`).
- `src/components/tabs/home/HomeSkeleton.jsx` — loading state, no blank screen on first entry.
- 관련 브랜치: `feature/ia-tab-restructure` (#10), `feature/home-relative-change` (#11) — both merged.
- 기술부채: none identified. 재사용 가능: `RelativeChangeCard`/`PatternInsightCard` already designed to be shared with REPORT (see item 4).

## 2. Timeline 통합 완료 (Timeline merge complete)

**상태: 완료 (기본 병합) — Decision Log 드릴다운까지 포함, seed 스크립트는 없음**

- `src/lib/mergeTimeline.js` — `mergeScansAndEvents(scans, events)`, single sort-merged, time-descending list. Used by both `TimelineModule.jsx` (full list) and `RecentTimelinePreview.jsx` (HOME's 3-item preview) — no separate ACTION screen exists.
- `src/components/EventDetailModal.jsx` + `src/lib/eventComparison.js` — tapping an event row opens a before/after Finger Score mini-graph (recharts), with an explicit "비교할 측정 기록이 더 필요합니다" fallback when there isn't enough data in the ±3-week window.
- 관련 브랜치: `feature/decision-log-drilldown` (#12), merged.
- 기술부채: **no seed script exists** (`tools/seed/` doesn't exist). All verification so far has been manual (simulated scans + the local offline-queue for events) since there's no live Firebase project configured locally (no `.env`). This is the master plan's own STEP 3 ask — flagged as genuinely new work, not already done.
- 리팩토링 필요: `TimelineModule.jsx` currently does its own `useState`/`useEffect` fetch of scans+events — this duplicates logic also present in `JOINTRUNShell.jsx` and `RecentTimelinePreview.jsx`. STEP 1's hook layer (`useTimelineData`) is intended to fix this.

## 3. Event Marker 저장 가능 (Event Marker can save)

**상태: 완료**

- `src/components/EventMarkerModal.jsx` — 8 fixed types (`src/lib/eventTypes.js`) + custom, tap-to-save for fixed types (2 taps total: open + type), custom requires a label + explicit save button.
- `src/lib/firestore.js` — `saveEvent()`/`getEventHistory()`/`flushPendingEvents()` with a `localStorage`-backed offline queue (`jointrun_pending_events`) for when Firestore is unreachable or unconfigured.
- 관련 브랜치: `feature/event-marker-ui` (#8), merged.
- 기술부채: `saveEvent()` is called directly from the modal — no repository seam yet (STEP 1/4 work).

## 4. Pattern Feedback 표시 (Pattern Feedback displayed)

**상태: 완료 — 문구는 마스터플랜과 100% 일치**

- `src/lib/detectPattern.js` — pure function, 4 states: `stable` / `declining` / `volatile` / `insufficient`, exactly matching the master plan's required states and even the exact Korean wording specified in STEP 5 ("최근 3주 평균과 유사합니다.", "최근 2주간 평소보다 뻣뻣한 날이 증가했습니다.", "최근 기록의 변동 폭이 평소보다 커졌습니다." ≈ "변동성이 증가했습니다.", "패턴을 판단하기엔 아직 기록이 부족합니다.").
- `src/components/PatternInsightCard.jsx` — rendered in both HOME (`HomeModule.jsx`) and REPORT (`ReportModule.jsx`), same component, same underlying `detectPattern()` call — no duplicate judgment logic.
- Copy-guideline string audit already performed twice this project (Items 4 and 6) with 0 violations found across all new strings.
- 관련 브랜치: `feature/pattern-feedback` (#13), merged.
- 기술부채: none. `detectPattern.js` isn't yet wrapped as an object named `PatternDetector` (STEP 1 asks for a "domain service" shape) — currently a plain exported function, which is functionally equivalent but not literally the shape the master plan names.

## 5. Report 조회 가능 (Report viewable)

**상태: 완료**

- `src/components/tabs/ReportModule.jsx` — biomarker list + `PatternInsightCard`, purely view-only. The PDF export button is present but `disabled` with label "다음 업데이트 예정" — no export/share actually wired.
- 관련 브랜치: `feature/ia-tab-restructure` (#10) initially disabled it, unchanged since.
- 기술부채: `ReportModule` receives `scans` as a prop threaded from `JOINTRUNShell` — same duplication concern as item 2, fixed by STEP 1's `useReportData` hook.

## 6. KPI 이벤트 연결 완료 (KPI events connected)

**상태: 완료 (5/5), 단 `timeline_created` 중복 방지는 client-side**

| 이벤트 | 위치 | 비고 |
|---|---|---|
| `return_scan` | `JOINTRUNShell.jsx:191` | scanCount≥1일 때만 발생 |
| `timeline_created` | `JOINTRUNShell.jsx:144` | scans≥1 & events≥1 & `!userProfile.timelineCreated`일 때 1회, Firestore profile 플래그로 중복 방지 |
| `event_marker_created` | `EventMarkerModal.jsx:27` | 저장 성공 직후 |
| `session_start` | 없음 (의도적) — GA4 `gtag`가 자동 수집하는 표준 이벤트라 커스텀 이벤트를 별도로 추가하지 않음 | `src/lib/analytics.js`의 `initGoogleAnalytics()` |
| `history_comparison_viewed` | `EventDetailModal.jsx:15` | `hasEnoughData`일 때만(데이터 부족 안내 화면 제외) |

- `src/lib/analytics.js`의 `trackKpiEvent(name, uid, params)` — 모든 이벤트에 `userId`(Firebase UID, PII 아님)+`timestamp` 공통 파라미터, PII 없음 확인됨.
- 관련 브랜치: `feature/kpi-analytics` (#9), merged.
- 기술부채: `timeline_created` 중복 방지가 **client-side**(Firestore profile 문서의 `timelineCreated` boolean)다. 마스터플랜/이전 작업지시서 모두 "서버 측(Cloud Function) 판정을 권장"이라고 명시하지만, 이 저장소엔 Cloud Functions 인프라 자체가 없다(`functions/` 디렉토리 없음, Firebase CLI 프로젝트 설정 없음). 동시 다발적 write(멀티 디바이스 동시 로그인 등)가 있으면 이론상 중복 발생 가능 — 실사용 규모에서 리스크는 낮지만 알려진 한계로 남겨둔다.
- 이 로컬 환경엔 살아있는 Firebase 프로젝트가 없어(`.env` 없음) Firebase DebugView로 직접 검증한 적은 없음 — 코드 리뷰 + 로직 단위 확인으로 대체됨.

**STEP 7 재검증(마스터플랜, 2026-07-14):** STEP 1~6의 Repository/Hook 리팩토링을 모두 거친 뒤, `window.gtag`를 스텁으로 교체해 브라우저에서 실제 흐름으로 재확인함.
- 스캔 1회(첫 스캔) → 이벤트 없음(정상, scanCount였던 0에서 게이팅됨)
- 스캔 2회째 → `return_scan` 1회 발생
- 이벤트 1건 저장(병원 방문) → `event_marker_created` 1회 + `timeline_created` 1회(정확히 이 시점에만) 발생
- 이벤트 2건째 저장(운동 시작) → `event_marker_created` 1회 추가 발생, **`timeline_created`는 재발생하지 않음**(누적 카운트 1 유지) — North Star 중복 방지 요구사항 충족 확인
- 콘솔 에러 없음

## 7. Architecture Review 완료 (Critical Issue 0)

**상태: 미착수** — 이 마스터플랜의 STEP 8 자체가 아직 수행 전. 이 audit 문서가 그 전 단계.

## 8. SCAN 기능 정상 동작 (회귀 없음)

**상태: 완료 — 변경 이력 없음**

- `src/components/MotionScanPage.jsx`의 마지막 커밋은 `3754977`(브랜드 컬러 변경, 로직 변경 아님) — 이후 모든 IA/이벤트/패턴 작업에서 이 파일은 건드리지 않음(`git log` 확인).
- 매 항목마다 시뮬레이션 스캔으로 end-to-end 확인함(2번, 3번, 4번, 6번 작업 시).

---

## 다음 단계 우선순위 제안

1. **STEP 0.5 (Design System) + STEP 1 (Repository/Hooks)** 부터 시작 — 이미 동작하는 기능(1~6번)을 재구현하지 않고, 중복된 fetch 로직(`JOINTRUNShell`/`TimelineModule`/`RecentTimelinePreview`에 흩어진 `useState`+`useEffect`)만 hook으로 정리하는 게 가장 비용 대비 가치가 크다.
2. **STEP 3의 seed 스크립트**가 유일하게 이 리포에 정말 없는 기능이다 — 우선순위 있게 챙길 것.
3. **`timeline_created` 서버 측 판정(Cloud Function)**은 이번 스프린트 범위 밖(인프라 자체가 없음)이지만, 다음 스프린트에서 정식으로 다룰 항목으로 별도 기록해둘 가치가 있다.
4. STEP 8(Architecture Review)은 계획대로 맨 마지막에 — 지금 아키텍처는 아직 리팩토링 전이라 미리 봐도 의미가 없다.
