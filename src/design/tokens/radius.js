// Claude Design 토큰(RC1): card 18/24, btn 12, chip 999.
import tokens from "./tokens.json";

export const radius = {
  card: tokens.radius.card,
  cardLarge: tokens.radius.cardLarge,
  control: tokens.radius.btn,
  full: tokens.radius.chip,
};
